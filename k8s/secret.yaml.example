apiVersion: v1
kind: Secret
metadata:
  name: desktop-pet-secrets
type: Opaque
stringData:
  openai_api_key: "your-openai-api-key-here"
  claude_api_key: "your-claude-api-key-here"
---
# 使用方法：
# 1. 复制此文件为 secret.yaml
# 2. 替换上面的API密钥
# 3. 运行: kubectl apply -f k8s/secret.yaml
# 注意：不要将包含真实密钥的secret.yaml提交到版本控制

